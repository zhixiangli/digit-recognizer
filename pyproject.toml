[project]
name = "digit-recognizer"
version = "1.0.0"
description = "Use CNN to achieve 99% in digit recognizer"
requires-python = ">=3.12"
dependencies = [
    "tensorflow>=2.16",
    "numpy>=1.26",
    "pandas>=2.2",
    "matplotlib>=3.9",
]
